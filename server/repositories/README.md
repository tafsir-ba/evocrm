# Repositories

Data access layer. All workspace-owned queries must include server-resolved `workspaceId`.

Use `withWorkspaceScope()` from `/server/workspaces/with-workspace-scope.ts`.

Phase 0: placeholder only. Domain repositories are added in later phases.
